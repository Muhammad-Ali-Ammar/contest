#include <bits/stdc++.h>
#pragma GCC optimize("O3", "unroll-loops")
using namespace std;

int main() {
  /*.......................................................................*/
  /************************** permutation **********************/
  /*.......................................................................*/

  vector<int> v = {10, 20, 30, 40, 50};
  sort(v.begin(), v.end());

  do {
  } while (next_permutation(v.begin(), v.end())); 

  
}